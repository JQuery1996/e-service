import { ProfileTemplete } from "components/templetes";
import { FC } from "react";
export interface ProfilePageProps {}

const ProfilePage: FC<ProfilePageProps> = () => {
  return <ProfileTemplete />;
};
export default ProfilePage;
