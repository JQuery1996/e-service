import { RegisterTemplete } from "components/templetes";
import { FC } from "react";

export interface RegisterProps {}

const RegisterPage: FC<RegisterProps> = () => {
  return <RegisterTemplete />;
};
export default RegisterPage;
